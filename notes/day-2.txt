Angular
--------------------------
Client-side JS framework
helps to develop an web app using a series of components
it helps you create Single Page Application (SPA)

features:
------------------
1. binding - data and event
2. signal for UI update
3. dependency injection framework
4. pass data between components easily
5. MV-* architecture
6. routing
7. HTTP request using built-in service
etc.

types of angular application:
	- application
	- library

modes of angular application:
	- feature module [>ng new modular-app --standalone=false]
	- standalone     [>ng new standalone-app [--standalone=true]]

Angular Assets:

asset type		type		decorator	compulsory
-----------------------------------------------------------------------
component		class		@Component	yes
direceive		class		@Directive	yes
pipe			class		@Pipe		yes	
service			class		@Injectable	no (better to use)

module			class		@NgModule	yes


registration of services:
1. component level
2. root level


properties of NgModule
-------------------------------
declarations: register the component, pipe and directives ONLY in this array 

imports: register the name of other feature modules of this application as well as built-in modules ONLY here in this array

exports: the component, directives, pipes or modules that are registered with this module can be re-exported through this property. it is a subset of the assets registered via declarations and imports property combined

bootstrap: ONLY the name of components (that are already registered with this module through declarations property) that should be loaded by default (bootstrapped) when the module is loaded

providers: register the services ONLY in this array 


properties of Component
------------------------------
imports: register the name of any component, directive, pipe or module (built-in module or child module) ONLY (not services)
Note: ONLY available as property if the component is standlone (standalone:true)

selector: name of the custom tag of the component
template: the design in string format
templateUrl: the path of the HTML template file
styles: the CSS styles in an array
styleUrl: the path of the single CSS file
styleUrls: the path of the CSS files in an array
providers: to register a service ONLY to be used for this component
standlone: true/false

@angular/core => Component, NgModule, PlatformRef etc.
@angular/platform-browser => bootstrapApplication(), platformBrowser()


@angular/forms
@NgModule({
	declarations:[NgModelDirective, NgFormDirective],
	exports:[NgModelDirective, NgFormDirective]

})
export class FormsModule{
}

@Directive({
	selector:'[ngModel]',
	standalone:false
})
export class NgModelDirective{
}

@Directive({
	selector:'[ngForm]',
	standalone:false
})
export class NgFormDirective{
	controls:FormControl[]
}



input controls and binding
--------------------------------
1. using data and event binding on the same input control
<input type="text" [value]="prop-name" (input) = "method($event.target.value)"/>

2. using signal [data and event binding on the same input control]
<input type="text" [value]="some-signal-prop()" (input) = "some-signal-prop.set($event.target.value)"/>


3. using ngModel ( a directive from FormsModule) and ywo way binding
<input type="text" [(ngModel)]="prop-name" name="some-name"/>

Note: 
1. ngModel directive (a custom attribute understood ONLY by Angular) creates a FormControl object and connects one end to the <input> and other end to a property in the component code. the value of the property through that formcontrol flows to the <input> and any change in the value of the input triggers an event of the formcontrol which in turn updates the same property in the component

2. you MUST use "name" attribute of the input element. when the FormControl object is created it is referred by that "name" value.

4. using forms
	
	a. template-driven form: manage the form data through the template (design/view)
	use some forms directives such as ngForm, ngModel etc. in the template that helps you to access the whole form element and individual controls and their data in code
	package: @angular/forms
	module: FormsModule 
	directives: ngModel, ngForm
	types: FormControl, FormGroup, NgForm etc. [@angular/forms]


	b. reactive form: manage the form through the component code
	package: @angular/forms
	module: ReactiveFormsModule 
	directives: formGroup, formControl, formControlName, formGroupName
	types: FormControl, FormGroup [@angular/forms]

pms-app

app
	app.ts
	app.html
	app.css
	app.spec.ts
	app.config.ts

	modules (folder)
		auth (folder)
			auth-module.ts (file -> AuthModule)
			componnents (folder)
				login (folder)
					login.ts	
					...
					...
				register (folder)
					register.ts
					....


		products (sub-folder)
			products-module.ts (module file) -> ProdutsModule
			components (sub-folder)
				product-list (sub-folder)
					product-list.ts
					product-list.html
					product-list.css
				product-detail
					product-detail.ts
					product-detail.html
					product-detail.css
	
	productlist, productdetail, addproduct, editproduct

		shared (folder)
			shared-module.ts (file -> SharedModule)
			components
				home
					home.ts
					home.html
					home.css
			dashboard, home, pagenotfound


	













